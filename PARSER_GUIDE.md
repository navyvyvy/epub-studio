# EPUB Studio 제목 파서 가이드

이 문서는 `index.html`의 Web Worker 안에 있는 제목 감지 로직을 설명합니다. 제목 형식을 추가하거나 오탐을 고칠 때는 이 문서와 `tests/smoke.test.mjs`를 함께 수정합니다.

## 한눈에 보는 처리 순서

```text
TXT 줄 분리
  -> 줄 정규화와 길이 제한
  -> 명백한 본문·잡음 제외
  -> 내장 패턴 또는 커스텀 정규식으로 그룹 분류
  -> 그룹별 숫자 흐름 검증
  -> 단일/다중 패턴에 맞는 활성 그룹 선택
  -> 앞뒤 제목 번호를 이용한 범위 필터
  -> 중복 제거
  -> 괄호 번호형 후보 보정
  -> 사용자가 제외한 줄 반영
  -> 미리보기 또는 EPUB 목차 생성
```

정규식에 한 번 맞았다고 바로 제목이 되지는 않습니다. 패턴 일치, 문장 제외, 숫자 흐름, 그룹 우선순위를 모두 통과해야 최종 목차가 됩니다.

## 입력값

`ClusterParser.analyze()`가 판단에 사용하는 값은 다음과 같습니다.

| 값 | 역할 |
| --- | --- |
| `lines` | TXT를 줄 단위로 나눈 원문 |
| `useMultiToc` | 여러 제목 그룹을 함께 쓸지 결정 |
| `fileName` | `1-200` 같은 예상 화수 범위를 읽을 때 사용 |
| `userRegex` | 고급 설정의 커스텀 정규식 |
| `preserveCustomMatches` | 기본 정규식을 사용자가 수정했는지 나타냄 |

미리보기에서 선택한 다른 형식 후보와 제외한 줄은 `analyze()` 다음 단계에서 반영됩니다. 같은 처리가 미리보기, 유효성 검사, 실제 변환에 모두 적용됩니다.

## 1. 줄 정규화와 기본 조건

`cleanLine()`은 다음 작업을 합니다.

- `&#x20;`, `&#32;`, `&nbsp;`를 일반 공백으로 바꿉니다.
- 제로 폭 문자와 BOM 문자를 제거합니다.
- 줄 앞뒤 공백을 제거합니다.
- 빈 줄과 90자를 넘는 줄은 기본 제목 분류에서 제외합니다.

강한 제목 시작 패턴이 아닌 일반 후보는 원칙적으로 위아래가 빈 줄이어야 합니다. 다음 항목은 이 제한을 완화합니다.

- `제1화`, `Chapter 1`, `#1`처럼 시작부터 제목 형식이 분명한 줄
- 올바르게 감싼 제목
- `프롤로그`, `후기` 같은 특수 제목
- 숫자만 있는 줄

## 2. 강한 제목 시작 패턴

`TITLE_RULES.strongStartPatterns`는 제목 가능성이 높은 줄을 먼저 표시합니다.

| 형식 | 예시 |
| --- | --- |
| 한국어 단위 | `제1화 시작`, `◈12화◈ 제목`, `300장 제목` |
| 영문 단위 | `Chapter 2`, `EP.3`, `Season 4` |
| 해시 번호 | `#1. 제목`, `＃109화 제목` |
| 숫자형 | `001 제목`, `1. 제목`, `1-2 제목`, `[3] 제목` |
| 감싼 제목 | `< 007. 제목 >`, `< 독방 - [3] >` |

강한 시작은 일부 문장 필터를 완화할 뿐입니다. `끝`, 타임코드, 프로필 행처럼 명백한 제외 대상은 강한 시작이어도 제목이 되지 않습니다.

## 3. 감싼 제목 정규식

감싼 제목은 역할이 다른 세 정규식으로 나뉩니다.

### `BRACKET_TITLE_PATTERN`

`< >`, `【 】`, `[ ]`, `( )` 안에 다음 내용이 있는 짧은 제목을 처리합니다.

- `제1화`, `2장`, `3수` 같은 한국어 번호 단위
- `Chapter 1`, `Episode 2` 같은 영문 번호 단위
- `프롤로그`, `에필로그`, `외전`, `후기`, `공지`, `서장`, `종장` 같은 특수 제목
- 짧은 숫자 제목
- 번호 뒤에 `-`, `:`, `.`, `·`로 붙는 짧은 부제

### `BRACKET_SUFFIX_TITLE_PATTERN`

제목 뒤에 대괄호 번호나 특수 구분자가 붙는 형식입니다.

```text
< 소환전 - [1] >
# 대학 - [서장] #
< 아스가르드 - [1] 수정본 >
```

### `WRAPPED_NUMBER_TITLE_PATTERN`

감싼 영역 안에서 숫자와 점 또는 닫는 괄호로 시작하는 형식입니다.

```text
< 006. 들린다 >
< 017. 내가 그렇게 만들 생각이다 >
```

`BRACKET_SUFFIX_TITLE_PATTERN`과 `WRAPPED_NUMBER_TITLE_PATTERN`은 명시적인 제목 표식으로 취급되어 단일 패턴에서도 기본 활성 그룹에 포함됩니다.

## 4. 제목 그룹 분류 순서

`classifyLine()`은 아래 순서대로 처음 일치한 그룹 하나를 선택합니다. 순서가 곧 분류 우선순위입니다.

| 순서 | 그룹 | 판단 기준 |
| --- | --- | --- |
| 1 | `CUSTOM` | 사용자가 기본 정규식을 수정했고 해당 정규식에 일치 |
| 2 | `KOR_*` | 줄 앞쪽에 `화`, `회`, `장`, `막`, `편`, `부`, `수` 단위가 있음 |
| 3 | `KOR_SUFFIX_*` | 줄 끝쪽에 한국어 번호 단위가 있음 |
| 4 | `ENG_*` | `Chapter`, `Stage`, `Episode`, `Ep`, `Part`, `Track`, `Season` |
| 5 | `SPECIAL` | 프롤로그, 에필로그, 외전, 후기, 공지, 서장, 종장, 프로필 등 |
| 6 | `BRACKET_SUFFIX` | 제목 뒤 `- [번호]` 또는 감싼 숫자 제목 |
| 7 | `BRACKET` | 일반 감싼 제목 |
| 8 | `SHARP` | `#` 또는 `＃`로 시작하는 번호 제목 |
| 9 | `NUMBER` | `1.`, `1_2`, `001`, `[1]`, 숫자 단독 제목 |
| 10 | `CUSTOM` | 수정하지 않은 기본 정규식만 일치한 경우 |

한국어 그룹은 단위별로 `KOR_화`, `KOR_장`처럼 나뉩니다. 영문 그룹도 `ENG_CHAPTER`, `ENG_EP`처럼 키워드별로 나뉩니다.

## 5. 제목에서 제외하는 기준

`shouldSkipCandidate()`는 다음 순서로 후보를 제거합니다.

1. `작가의 말`은 제외합니다. `후기`는 남깁니다.
2. `끝`으로 끝나는 줄은 이전 회차의 종료 표식으로 보고 제외합니다.
3. `[00:00:01]`, `00:00:03,500 --> ...` 같은 타임코드는 제외합니다.
4. `배우자 | ... (1904)` 같은 프로필·위키형 행은 제외합니다.
5. 괄호가 열리고 닫혔지만 허용된 감싼 제목 형식이 아니면 제외합니다.
6. 일반 인용문으로 시작하는 줄은 제외합니다.
7. `3화 때`, `5위계 정도`, `2장으로`처럼 번호 단위 뒤에 조사가 이어지면 본문으로 봅니다.
8. `곧바로 3화.`처럼 문장 안에서 회차를 언급하는 줄은 제외합니다.
9. 문장형 종결 표현이 있는 긴 줄은 제외합니다. 강한 제목은 더 긴 길이까지 허용합니다.
10. 긴 괄호 문장에 쉼표나 마침표가 있으면 제외합니다. 명시적인 감싼 제목 형식은 예외입니다.
11. 번호 단위가 문장 뒤쪽에 있고 마침표로 끝나는 약한 후보는 제외합니다.
12. 날짜, 시간, 금액, 단위, 순위, 층수 같은 숫자 표현은 약한 후보에서 제외합니다.

대표적인 제외 예시는 다음과 같습니다.

```text
[00:00:01]
곧바로 3화.
5화 때 시청률이 올랐다.
아, 그…… <민중을 이끄는 자유의 여신(1830)> 말인가.
배우자 | 로웨나 진-로스차일드(1904)
< 80화 반시 (1) > 끝
```

## 6. 기본 커스텀 정규식의 역할

고급 설정에 보이는 기본 정규식은 전체 파서를 대신하지 않습니다. 내장 그룹에서 놓친 형식을 보완하는 호환용 후보 규칙입니다.

정규식은 크게 다음 분기로 구성됩니다.

| 분기 | 처리 대상 |
| --- | --- |
| 감싼 형식 | `<제1화 ...>`, `<프롤로그>`처럼 괄호 안에 번호나 특수 제목이 있음 |
| 한국어 번호 | `제1화`, `2회`, `3수`로 시작하는 제목 |
| 영문 번호 | `Chapter 1`, `Episode 2`, `Part 3` |
| 해시 번호 | `#1`, `＃2`로 시작하는 제목 |
| 일반 숫자 | 숫자로 시작하되 날짜, 단위, 금액, 순위 등의 금지 표현은 아닌 제목 |

마지막 일반 숫자 분기는 문장형 `고.`, `다.`, `자.`, `죠.`, `요.` 종결도 피합니다. 그 밖의 본문 오탐은 공통 제외 규칙과 숫자 흐름 검사에서 다시 걸러집니다.

사용자가 정규식을 수정하면 `preserveCustomMatches`가 켜집니다. 이때 `CUSTOM` 일치 항목이 하나라도 있으면 해당 그룹만 주 목차로 선택하고, 일치 항목이 없으면 내장 그룹 선택을 계속합니다. 커스텀 정규식도 90자 제한, 공통 제외 규칙, 약한 후보의 위아래 빈 줄 조건은 그대로 적용받습니다.

## 7. 숫자 추출 우선순위

`getChapterNum()`은 다음 순서로 대표 번호 하나를 찾습니다.

1. `제12화`, `12장` 같은 한국어 단위 번호
2. `Chapter 12`, `EP.12` 같은 영문 번호
3. `#12`, `＃12` 같은 해시 번호
4. 소수나 날짜가 아닌 첫 일반 숫자

한 줄에 숫자가 여러 개 있으면 그룹 전체에서 각 숫자 열을 따로 비교합니다. 예를 들어 제목마다 연도와 화수가 함께 있으면 가장 긴 제목 흐름을 만드는 숫자 열을 선택합니다.

## 8. 연속 번호 판정

`getTitleSequenceIndexes()`의 기본 근거는 `이전 번호 + 1`입니다.

- 처음에는 최소 두 개의 이어지는 번호가 있어야 흐름이 성립합니다.
- 중복 번호와 역행 번호는 건너뜁니다.
- 중간에 다른 숫자가 끼어도 뒤에서 기대 번호가 나오면 끼어든 숫자는 제외합니다.
- 흐름이 성립한 뒤 같은 패턴의 일부 회차가 누락된 경우, 10 이하의 단독 간격은 이어진 제목으로 허용합니다.
- 간격 뒤에 다시 두 개 이상의 연속 번호가 나오면 같은 패턴의 후속 구간으로 이어 붙입니다.

이 규칙 때문에 `3, 4, 7`처럼 같은 제목 형식이 유지되는 누락 회차는 살릴 수 있지만, `3, 7, 8, 9, 4`처럼 먼저 성립한 흐름을 방해하는 중간 숫자는 주 흐름으로 선택되지 않습니다.

`getContinuousSequenceIndexes()`는 서로 다른 그룹 사이에서 정확한 `+1` 흐름을 찾습니다. 단일 패턴에서는 `＃109화`, `＃110화`, `111화`처럼 표식이 바뀌어도 번호가 이어지면 하나의 임시 `SEQUENCE_*` 그룹으로 합칩니다.

## 9. 알려지지 않은 반복 형식 자동 감지

`discoverSequentialTitleGroups()`는 내장 정규식에 없는 형식도 문서 전체에서 찾습니다.

1. 50자 이하의 줄만 봅니다.
2. 각 숫자 앞뒤의 반복되는 단어와 구두점을 `{n}` 자리로 바꿔 형태 키를 만듭니다.
3. 같은 형태 키의 숫자가 이어지는지 검사합니다.
4. 이미 신뢰할 수 있는 내장 제목 흐름이 있으면 새 자동 그룹을 무리하게 추가하지 않습니다.

예를 들어 `Story@001 :: Alpha`, `Story@002 :: Beta`처럼 숫자 주변 표식이 반복되면 `AUTO_*` 그룹이 될 수 있습니다. 괄호 번호형은 이 단계가 아니라 별도 후보 단계에서 처리합니다.

## 10. 숫자 후보 보정

그룹 분류 뒤에는 다음 보정이 적용됩니다.

- 같은 그룹 안에서 가장 긴 숫자 흐름을 만드는 열만 남깁니다.
- `107장 특전대 (1)`, `107장 특전대 (2)`처럼 본 제목 번호가 같고 뒤쪽 부 번호가 증가하면 부 번호 흐름도 인정합니다.
- 앞형과 뒤형이 다른 한국어 제목이라도 단위가 같고 번호가 `+1`이면 같은 흐름으로 복구합니다.
- 확인된 제목 다음에 숫자만 있는 줄이 정확히 `+1`이면 같은 제목 그룹으로 이어 붙입니다.
- 강한 바깥 제목 흐름 사이에 낮은 우선순위의 무관한 숫자 흐름이 끼면 샌드위치 오탐으로 제거합니다.

## 11. 단일 패턴 선택 우선순위

단일 패턴에서는 `SPECIAL`과 `BRACKET_SUFFIX`를 기본으로 유지하고, 나머지 그룹 중 하나를 주 그룹으로 고릅니다.

파일명에 범위가 없을 때의 비교 순서는 다음과 같습니다.

1. 살아남은 제목 수가 많은 그룹
2. 개수가 같으면 파일에서 먼저 나온 그룹
3. 같은 위치에서 시작했다면 그룹 신뢰도

그룹 신뢰도는 다음과 같습니다.

```text
KOR, ENG > BRACKET, BRACKET_SUFFIX, SHARP > NUMBER > AUTO/CUSTOM 기타 그룹
```

파일명에 `1-200`, `1~200`, `1–200` 같은 범위가 있으면 마지막 범위를 읽어 다음 순서로 비교합니다.

1. 파일명 범위와 더 많이 겹치는 그룹
2. 겹침이 같으면 시작·끝 거리가 더 가까운 그룹
3. 범위 조건이 같으면 제목 수, 첫 등장 위치, 그룹 신뢰도

따라서 `1화`, `2화`, `3화`가 있다는 이유만으로 항상 주 목차가 되지는 않습니다.

## 12. 다중 패턴 선택

다중 패턴을 켜면 숫자 흐름과 오탐 필터를 통과한 모든 그룹을 활성화합니다. 단순히 정규식에 걸린 모든 줄을 넣는 방식은 아닙니다.

다중 패턴에서는 같은 번호라도 그룹이 다르면 각각 유지합니다. 단일 패턴에서는 같은 대표 번호와 부 번호가 겹치면 한 항목만 남깁니다.

## 13. 최종 범위와 중복 필터

최종 후보는 확인된 제목 번호를 기준점으로 사용합니다.

- 연속 번호가 확인된 항목과 강한 제목 그룹은 앵커가 됩니다.
- 약한 숫자 후보는 앞뒤 앵커 번호 범위 안에 있을 때만 남습니다.
- 앵커가 있는 상태에서 번호도 없는 일반 후보는 제외합니다.
- `SPECIAL`은 이 범위 검사와 관계없이 유지합니다.

중복 키는 다음처럼 만듭니다.

- 단일 패턴: 대표 번호와 뒤쪽 `(부 번호)`
- 다중 패턴: 그룹, 대표 번호, 뒤쪽 `(부 번호)`
- `BRACKET_SUFFIX`: 전체 제목 문자열

## 14. 다른 형식의 목차 후보

`findParenthesizedTitleFallback()`은 제목 본문 뒤에 `(1)`, `（2）` 같은 번호가 있는 형식을 찾습니다.

```text
내집 마련의 꿈(2)
콘티가 움직이더라 (1)
비료를 거름삼아 (1)
' 이 프로그램은 보고 계신 스폰서의 제공으로 보내드립니다(3)(무료 마지막화입니다!) '
```

후보 조건은 다음과 같습니다.

- 전체 줄은 70자 이하여야 합니다.
- 괄호 번호 앞에 한글, 영문, 한자, 일본어 중 하나가 있어야 합니다.
- 프로필·위키형 행과 `끝` 표식은 제외합니다.
- 괄호 번호 뒤에는 짧은 부제나 괄호형 안내 문구만 허용합니다.
- 공통 제외 규칙에서 문장형으로 탈락한 후보는 감싼 형식이거나 같은 제목 바탕이 반복될 때만 다시 허용합니다.

이 후보는 단일·다중 설정과 관계없이 미리보기에 별도로 표시됩니다. 사용자가 추가하면 실제 변환 목차에도 같은 줄이 반영됩니다.

다른 형식이 기존 주 목차보다 확실히 많으면 자동으로 주 목차가 됩니다.

- 기존 감지 결과가 최소 2개 있어야 비교합니다.
- 같은 괄호 번호 형식의 후보 수가 기존 결과의 2배 이상이어야 합니다.
- 동시에 기존 결과보다 최소 3개 이상 많아야 합니다.
- 단일 패턴에서는 기존 일반 그룹을 대체하되 `SPECIAL`은 유지합니다.
- 다중 패턴에서는 기존 결과에 합칩니다.
- 사용자가 커스텀 정규식을 수정한 상태에서는 자동 승격하지 않습니다.

감싼 후보와 일반 후보는 서로 다른 형식으로 집계합니다. 후보 하나가 이미 기본 파서에 잡혔다면 같은 제목 바탕을 가진 앞뒤 후보도 함께 목차로 올립니다.

## 15. 미리보기 선택과 실제 변환

파서 결과 이후에 다음 순서로 사용자 선택을 적용합니다.

1. 자동 감지 결과를 만듭니다.
2. 자동 승격 또는 사용자가 추가한 다른 형식 후보를 넣습니다.
3. 사용자가 `X`로 제외한 줄을 뺍니다.
4. 남은 줄을 미리보기, 유효성 검사, EPUB 챕터 분할에 동일하게 사용합니다.

원본 TXT는 수정하지 않습니다. 후보 추가와 제외 상태는 현재 페이지의 파일 항목에만 유지됩니다.

## 16. 패턴을 추가할 때

새 사례는 다음 순서로 처리합니다.

1. `tests/smoke.test.mjs`에 잡혀야 하는 줄과 잡히면 안 되는 비슷한 줄을 함께 추가합니다.
2. 기존 그룹으로 설명할 수 있으면 `strongStartPatterns` 또는 해당 `groupMatchers`만 좁게 수정합니다.
3. 명백한 본문 오탐이면 개별 그룹이 아니라 `shouldSkipCandidate()`의 공통 제외 규칙에 넣습니다.
4. 새 그룹이 꼭 필요한 경우에만 `groupMatchers`와 `getGroupTier()`를 함께 검토합니다.
5. 괄호 번호형이면 기본 그룹보다 `findParenthesizedTitleFallback()`에서 처리할 수 있는지 먼저 확인합니다.
6. 단일·다중 패턴을 모두 테스트합니다.
7. README와 메인 화면 기능 소개가 달라졌다면 함께 수정합니다.

검사는 다음 명령으로 실행합니다.

```powershell
node --test tests/smoke.test.mjs
```

## 17. 현재 한계

- 이 파서는 자연어 의미를 완전히 이해하는 모델이 아니라 규칙과 반복 증거를 조합한 휴리스틱입니다.
- 한 번만 등장하는 새로운 형식은 강한 제목 표식이나 사용자 선택이 없으면 놓칠 수 있습니다.
- 숫자가 많고 제목과 본문의 형식이 비슷한 문서는 미리보기 확인이 필요합니다.
- 길이 제한과 숫자 간격 기준을 넓히면 감지는 늘지만 본문 오탐도 함께 늘어납니다.

새 샘플이 생기면 임계값을 먼저 넓히기보다 해당 샘플과 가장 가까운 오탐 사례를 테스트에 함께 남기는 방식으로 보완합니다.
